const moment = require('moment');

// const date = moment();
// console.log(date.format('MMM Do YYYY LTS'));
// console.log(date.format('LT'));

const someTimeStamp = moment().valueOf();
console.log('someTimeStamp: ', someTimeStamp);
